server:
  port: 7099
spring: 
  application:
    name: mint-config
  cloud: 
    config: 
      label: master 
      server: 
        default-application-name: mint-config
        git: 
          uri: https://github.com/chuningfan/mintConfig.git
          search-paths: config
          username:
          password: